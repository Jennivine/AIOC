#include <bits/stdc++.h>
using namespace std;

//adjacency list of characters and their children
vector<int> charTree[3000000];

int pointer;
char input;

int main () {
	freopen("chains.in", "r", stdin);
	freopen("chains.out","w",stdout);
 
	scanf("%c", &input);
	while (input != '.') {
		if (input != '\n' && input != '\r') {
			// read into the adj list


		}
		scanf("%c", &input);
	}

	// char with no 

}
